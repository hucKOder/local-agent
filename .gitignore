# Provider credentials never belong in the repo.
auth.json
**/*auth.json
*.env
.env*
# omp stores provider logins here (~/.omp/agent/agent.db).
agent.db*

# Local agent session state in this workspace, not setup.
/.omo/

# Package install artifacts. Do not vendor these.
node_modules/
package-lock.json
